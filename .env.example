JMAP_SESSION_URL="https://mail.rotko.net/.well-known/jmap"

# single account (backward compatible)
JMAP_BEARER_TOKEN="username:password"

# multi-account (one per line, format: username:password)
# JMAP_BEARER_TOKENS="tommi:app password one
# hq:app password two
# noc:app password three
# peering:app password four"
